import glob

path = glob.glob("/tmp/tmp/*")
with open(path) as f:
    for line in f:
        if line.startswith('_cell_formula_units_Z'):
            print(path)
            print(line, end='')
        if not line
            print(path)
            print("NONE")
        
